set $mod Mod4
set $mod1 Mod1

font pango:Geist Medium 9

exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet

floating_modifier $mod

tiling_drag modifier titlebar

# starting polybar
# exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# polybar toggle
# bindsym $mod+p exec polybar-msg cmd toggle

# polybar restart :)
bindsym $mod+Shift+p exec polybar-msg cmd restart

# restarting dunst
exec_always --no-startup-id killall dunst

# assigning workspaces
for_window [instance="__scratchpad"] move scratchpad

for_window [class="VirtualBox Machine"] move container to workspace $ws4
for_window [class="org.gnome.Nautilus"] floating enable resize set 800 600, move absolute position center
for_window [class="feh"] floating enable resize set 800 450, move absolute position center
for_window [class="f_kitty"] floating enable resize set 999 600, move absolute position center, move scratchpad, scratchpad show
for_window [class="Blueberry.py"] floating enable resize set 999 600, move absolute position center
for_window [class="steam_app_0"] move container to workspace $ws1
for_window [class="libreoffice"] move container to workspace $ws8
assign [class="io.github.celluloid_player.Celluloid"] $ws1
assign [class="VSCodium"] $ws1
assign [class="Code"] $ws1
assign [class="firefox"] $ws2
assign [class="Google-chrome"] $ws2
assign [class="discord"] $ws3
assign [class="vesktop"] $ws3
assign [class="obsidian"] $ws3
assign [class="Spotify"] $ws9
assign [class="YouTube Music"] $ws9
assign [class="obs"] $ws10

set $wallpaper "$HOME/wallpapers/space.jpg"

exec_always picom --config ~/.config/picom/picom
exec_always xrandr --output Virtual-1 --mode 1360x768
exec_always feh --bg-fill $wallpaper
bindsym $mod+Shift+w exec feh -z --bg-fill "$HOME/wallpapers"
bindsym $mod+Ctrl+w exec feh --bg-fill $wallpaper

# exec_always --no-startup-id killall back4.sh
# exec_always --no-startup-id back4.sh 0.03 ~/gifs/6604090.gif

# mouse sensibility (edit last value to change)
exec xinput set-prop "Logitech G403 HERO Gaming Mouse" "Coordinate Transformation Matrix" 1, 0, 0, 0, 1, 0, 0, 0, 1.5

# laptop settins
exec_always xinput set-prop "ATML3000:00 03EB:2168 Touchpad" "libinput Tapping Enabled" 1
exec_always xinput set-prop "ATML3000:00 03EB:2168 Touchpad" "libinput Natural Scrolling Enabled" 1

# emojis 😁
bindsym $mod+period exec --no-startup-id rofi -modi emoji -show emoji -emoji-format '{emoji}' -config ~/.config/rofi/config-multicolumns.rasi

# scratchpad
bindsym $mod+Shift+minus floating enable resize set 900 550, move absolute position center move scratchpad
bindsym $mod+minus scratchpad show

# switching workspaces
bindsym $mod+Ctrl+Right workspace next
bindsym $mod+Ctrl+Left workspace prev
bindsym $mod+Tab workspace back_and_forth
bindsym $mod+Ctrl+Tab workspace next

# show time
bindsym $mod+t exec dunstify -r 99998 Status "$(~/scripts/notify-status)"

# change focus between tiling / floating windows
bindsym $mod+Space focus mode_toggle

# clipboard history
exec --no-startup-id greenclip daemon>/dev/null
bindsym $mod+v exec --no-startup-id rofi -modi "clipboard:greenclip print" -show clipboard

# screenshots
bindsym --release Print exec scrot -q 80 "/home/$USER/pictures/screenshots/%F_%T_$wx$h.jpg" -e 'xclip -selection clipboard -target image/png -i $f'
bindsym --release Ctrl+Print exec scrot -s -f -q 100 "/home/$USER/pictures/screenshots/%F_%T_$wx$h.jpg" -e 'xclip -selection clipboard -target image/png -i $f'
bindsym --release $mod1+Sys_Req exec scrot -u -q 100 "/home/$USER/pictures/screenshots/%F_%T_$wx$h.jpg" -e 'xclip -selection clipboard -target image/png -i $f'

# mute/unmute mic
bindsym $mod+Shift+m exec ~/scripts/mic-toggle
bindsym $mod+Shift+n exec ~/scripts/notification-toggle

# display last notification
bindsym $mod+n exec dunstctl history-pop

# media keys
bindsym Shift+XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +3.7% +5% && ~/scripts/notify-audio
bindsym Shift+XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -3.7% -5% && ~/scripts/notify-audio
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5% +5% && ~/scripts/notify-audio
bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5% -5% && ~/scripts/notify-audio
bindsym XF86AudioMute exec pactl set-sink-mute 0 toggle
bindsym XF86AudioPlay exec playerctl play-pause
bindsym $mod+p exec playerctl play-pause
bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# restart pipewire
bindsym $mod+Shift+a exec systemctl --user restart wirepluber pipewire pipewire-pulse

# brightness
bindsym XF86MonBrightnessUp exec --no-startup-id light -A 5 && ~/scripts/notify-brightness
bindsym XF86MonBrightnessDown exec --no-startup-id light -U 5 && ~/scripts/notify-brightness

# screekeys
bindsym $mod+Shift+s exec ~/scripts/toggle-screenkey

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Shift+Return exec i3-sensible-terminal --class f_kitty
bindsym $mod+Ctrl+Return exec i3-sensible-terminal --class f_kitty

# kill focused window
bindsym $mod+Shift+q kill
bindsym $mod+d exec "rofi -modi drun,run -show drun -show-icons"

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+ccedilla focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+ccedilla move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle resize set 900 550, move absolute position center

# center current window
bindsym $mod+c move absolute position center

# focus the parent container
bindsym $mod+a focus parent

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1 workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2 workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3 workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4 workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5 workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6 workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7 workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8 workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9 workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10 workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height.
    # Pressing down will grow the window’s height.
    bindsym j resize shrink width 10 px
    bindsym k resize grow height 10 px
    bindsym l resize shrink height 10 px
    bindsym ccedilla resize grow width 10 px

    # same bindings, but for the arrow keys
    bindsym Ctrl+Left resize shrink width 100 px or 100 ppt
    bindsym Ctrl+Down resize grow height 100 px or 100 ppt
    bindsym Ctrl+Up resize shrink height 100 px or 100 ppt
    bindsym Ctrl+Right resize grow width 100 px or 100 ppt

    # same bindings, but for the arrow keys
    bindsym Left resize shrink width 10 px
    bindsym Down resize grow height 10 px
    bindsym Up resize shrink height 10 px
    bindsym Right resize grow width 10 px

    # back to normal: Enter or Escape or $mod+r
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# gaps
gaps inner 3
gaps outer 0

# removing borders
default_border none
default_floating_border none

# color shemes for windows
set $bgcolor    #523d64
set $in-bgcolor #363636
set $text       #ffffff
set $u-bgcolor  #ff0000
set $indicator  #a8a3c1
set $in-text    #969696
#                       border          background      text            indicator (a line which shows where the next window will be placed)
client.focused          $bgcolor        $bgcolor        $text           $indicator
client.unfocused        $in-bgcolor     $in-bgcolor     $in-text        $in-bgcolor
client.focused_inactive $in-bgcolor     $in-bgcolor     $in-text        $in-bgcolor
client.urgent           $u-bgcolor      $u-bgcolor      $text           $u-bgcolor
