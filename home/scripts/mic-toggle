#!/bin/bash

pactl set-source-mute 1 toggle
pactl get-source-mute 1 | awk '/Mute: sim/ { system("notify-send -r 99999 -a MicToggle -u critical \"  Microfone Silenciado\"") } /Mute: não/ { system("notify-send -a MicToggle -r 99999 -u low \"  Microfone Ativo\"") }'
